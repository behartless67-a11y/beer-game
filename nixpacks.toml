[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = [
  'npm install',
  'cd client && npm install && cd ..',
  'cd server && npm install && cd ..'
]

[phases.build]
cmds = [
  'npm run build'
]

[start]
cmd = 'npm run start:production'
