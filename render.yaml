services:
  - type: web
    name: beer-game
    runtime: node
    buildCommand: npm run build
    startCommand: npm run start:production
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
